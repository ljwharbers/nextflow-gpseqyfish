// Config for running minimal tests

params {
	// input and output
	indir = "${baseDir}/data/"
	outdir = "${baseDir}/results"

	tif = true
	dapi = "dapi"
	yfish = "a647"
	max_memory = "3.GB"
	max_cpus = 1
	max_time = "1.h"
}
