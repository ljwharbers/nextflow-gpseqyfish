// Config for running minimal tests

params {
	// input and output
	indir = "${baseDir}/data/1min/"
	outdir = "${baseDir}/results"

	tif = true

	max_memory = "3.GB"
	max_cpus = 1
	max_time = "1.h"
}
